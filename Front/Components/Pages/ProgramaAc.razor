@page "/dashboard/programa-ac"
@rendermode InteractiveServer
@inject FrontBlazor_AppiGenericaCsharp.Services.ApiService Api
<<<<<<< HEAD

<PageTitle>Programas AC</PageTitle>

<div class="container mt-4">
    <div class="row mb-4">
        <div class="col-md-8">
            <h3>Gestión de Programas de Acreditación</h3>
        </div>
        <div class="col-md-4 text-end">
            @if (!mostrarFormulario)
            {
                <button class="btn btn-primary" @onclick="NuevoRegistro">
                    <i class="bi bi-plus-circle"></i> Nuevo
                </button>
            }
        </div>
=======
@inject FrontBlazor_AppiGenericaCsharp.Services.AuthSessionService Sesion
@inject NavigationManager Nav

<PageTitle>Programa - Área Conocimiento</PageTitle>

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3><i class="bi bi-award"></i> Programa - Área de Conocimiento</h3>
        @if (!mostrarFormulario && Sesion.EsAdmin)
        {
            <button class="btn btn-primary" @onclick="NuevoRegistro">
                <i class="bi bi-plus-circle"></i> Nueva Relación
            </button>
        }
>>>>>>> SantiagoEcheverriDev
    </div>

    @if (!string.IsNullOrEmpty(mensaje))
    {
        <div class="alert @(exito ? "alert-success" : "alert-danger") alert-dismissible fade show">
<<<<<<< HEAD
            @mensaje
            <button type="button" class="btn-close" @onclick="() => mensaje = string.Empty"></button>
=======
            @mensaje <button type="button" class="btn-close" @onclick="() => mensaje = string.Empty"></button>
>>>>>>> SantiagoEcheverriDev
        </div>
    }

    @if (mostrarFormulario)
    {
        <div class="card mb-4 border-primary">
            <div class="card-header bg-primary text-white">
<<<<<<< HEAD
                <h5 class="mb-0">@(editando ? "Editar Programa AC" : "Nuevo Programa AC")</h5>
            </div>
            <div class="card-body">
                <form @onsubmit="GuardarRegistro">
                    <div class="mb-3">
                        <label class="form-label">Nombre <span class="text-danger">*</span></label>
                        <input class="form-control" @bind="campoNombre" placeholder="Ej: Programa de Acreditación Básica" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Código</label>
                        <input class="form-control" @bind="campoCodigo" placeholder="Ej: AC-001" disabled="@editando" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Descripción</label>
                        <textarea class="form-control" rows="3" @bind="campoDescripcion" placeholder="Descripción..."></textarea>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Estado</label>
                        <select class="form-select" @bind="campoEstado">
                            <option value="activo">Activo</option>
                            <option value="inactivo">Inactivo</option>
                        </select>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success" disabled="@cargando">
                            @if (cargando)
                            {
                                <span class="spinner-border spinner-border-sm me-2"></span>
                            }
=======
                <h5 class="mb-0">Nueva Relación Programa - Área</h5>
            </div>
            <div class="card-body">
                <form @onsubmit="GuardarRegistro">
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="form-label">ID Programa <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" @bind="campoPrograma" required />
                            <small class="text-muted">Ver tabla <a href="/dashboard/programa">Programas</a></small>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label">ID Área Conocimiento <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" @bind="campoAreaConocimiento" required />
                            <small class="text-muted">Ver tabla <a href="/dashboard/area-conocimiento">Áreas</a></small>
                        </div>
                    </div>
                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-success" disabled="@cargando">
                            @if (cargando) { <span class="spinner-border spinner-border-sm me-1"></span> }
>>>>>>> SantiagoEcheverriDev
                            Guardar
                        </button>
                        <button type="button" class="btn btn-secondary" @onclick="Cancelar">Cancelar</button>
                    </div>
                </form>
            </div>
        </div>
    }

<<<<<<< HEAD
    <div class="card mb-4">
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label">Buscar por nombre</label>
                    <input type="text" class="form-control" @bind="busqueda" @bind:event="oninput" placeholder="Escribe para filtrar..." />
                </div>
                <div class="col-md-3">
                    <label class="form-label">Límite de registros</label>
                    <input type="number" class="form-control" @bind="limite" min="5" max="100" />
                </div>
                <div class="col-md-3 d-flex align-items-end">
                    <button class="btn btn-outline-primary w-100" @onclick="CargarRegistros">
                        <i class="bi bi-arrow-clockwise"></i> Cargar
                    </button>
                </div>
            </div>
=======
    <div class="card mb-3">
        <div class="card-body d-flex gap-2 align-items-end">
            <div>
                <label class="form-label">Límite</label>
                <input type="number" class="form-control" style="width:100px" @bind="limite" min="5" max="200" />
            </div>
            <button class="btn btn-outline-primary" @onclick="CargarRegistros"><i class="bi bi-arrow-clockwise"></i> Cargar</button>
>>>>>>> SantiagoEcheverriDev
        </div>
    </div>

    @if (cargando)
    {
<<<<<<< HEAD
        <div class="text-center py-5">
            <div class="spinner-border text-primary" role="status">
                <span class="visually-hidden">Cargando...</span>
            </div>
        </div>
    }

    @if (!cargando && registros.Any())
=======
        <div class="text-center py-4"><div class="spinner-border text-primary" role="status"></div></div>
    }
    else if (registros.Any())
>>>>>>> SantiagoEcheverriDev
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead class="table-dark">
<<<<<<< HEAD
                    <tr>
                        <th>ID</th>
                        <th>Nombre</th>
                        <th>Código</th>
                        <th>Descripción</th>
                        <th>Estado</th>
                        <th>Creado</th>
                        <th>Acciones</th>
                    </tr>
=======
                    <tr><th>Programa (ID)</th><th>Área Conocimiento (ID)</th><th>Acciones</th></tr>
>>>>>>> SantiagoEcheverriDev
                </thead>
                <tbody>
                    @foreach (var reg in registros)
                    {
                        <tr>
<<<<<<< HEAD
                            <td class="fw-bold text-primary">@reg["id"]</td>
                            <td>@reg["nombre"]</td>
                            <td><span class="badge bg-info">@(reg["codigo"] ?? "N/A")</span></td>
                            <td>
                                @{var desc = reg["descripcion"]?.ToString() ?? "";
                                  var descCorta = desc.Length > 50 ? desc[..50] + "..." : desc;}
                                <small class="text-muted">@descCorta</small>
                            </td>
                            <td>
                                @{var estado = reg["estado"]?.ToString() ?? "activo";
                                  var badge = estado == "activo" ? "bg-success" : "bg-danger";}
                                <span class="badge @badge">@estado</span>
                            </td>
                            <td><small class="text-muted">@(reg["fecha_creacion"] is DateTime dt ? dt.ToString("dd/MM/yyyy") : "N/A")</small></td>
                            <td>
                                <button class="btn btn-warning btn-sm me-2" @onclick="() => EditarRegistro(reg)"><i class="bi bi-pencil"></i> Editar</button>
                                <button class="btn btn-danger btn-sm" @onclick="() => ConfirmarEliminar(reg)"><i class="bi bi-trash"></i> Eliminar</button>
=======
                            <td>@reg.GetValueOrDefault("programa")</td>
                            <td>@reg.GetValueOrDefault("area_conocimiento")</td>
                            <td>
                                @if (Sesion.EsAdmin)
                                {
                                    <button class="btn btn-danger btn-sm" @onclick="() => EliminarRegistro(reg)"><i class="bi bi-trash"></i> Eliminar</button>
                                }
>>>>>>> SantiagoEcheverriDev
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
<<<<<<< HEAD

    @if (!cargando && !registros.Any())
    {
        <div class="alert alert-warning text-center">
            <i class="bi bi-inbox"></i> No se encontraron registros. @if (!mostrarFormulario) { <a href="#" @onclick="NuevoRegistro">Crear el primer registro</a> }
        </div>
=======
    else
    {
        <div class="alert alert-info text-center"><i class="bi bi-inbox"></i> No hay relaciones registradas.</div>
>>>>>>> SantiagoEcheverriDev
    }
</div>

@code {
    private List<Dictionary<string, object?>> registros = new();
    private bool cargando = true;
    private bool mostrarFormulario = false;
<<<<<<< HEAD
    private bool editando = false;
    private string mensaje = string.Empty;
    private bool exito = false;
    private string campoNombre = string.Empty;
    private string campoCodigo = string.Empty;
    private string campoDescripcion = string.Empty;
    private string campoEstado = "activo";
    private int? limite = 20;
    private string busqueda = string.Empty;

    protected override async Task OnInitializedAsync()
    {
=======
    private string mensaje = string.Empty;
    private bool exito = false;
    private int campoPrograma = 0;
    private int campoAreaConocimiento = 0;
    private int? limite = 20;

    protected override async Task OnInitializedAsync()
    {
        if (!Sesion.EstaAutenticado) { Nav.NavigateTo("/login", true); return; }
>>>>>>> SantiagoEcheverriDev
        await CargarRegistros();
    }

    private async Task CargarRegistros()
    {
        cargando = true;
<<<<<<< HEAD
        try
        {
            registros = await Api.ListarAsync("programa_ac", limite);
            if (!string.IsNullOrEmpty(busqueda))
            {
                registros = registros.Where(r =>
                    r["nombre"]?.ToString()?.Contains(busqueda, StringComparison.OrdinalIgnoreCase) ?? false
                ).ToList();
            }
        }
        catch (Exception ex)
        {
            mensaje = $"Error al cargar: {ex.Message}";
            exito = false;
        }
        finally
        {
            cargando = false;
        }
    }

    private void NuevoRegistro()
    {
        editando = false;
        campoNombre = string.Empty;
        campoCodigo = string.Empty;
        campoDescripcion = string.Empty;
        campoEstado = "activo";
        mostrarFormulario = true;
        mensaje = string.Empty;
    }

    private void EditarRegistro(Dictionary<string, object?> reg)
    {
        editando = true;
        campoNombre = reg["nombre"]?.ToString() ?? "";
        campoCodigo = reg["codigo"]?.ToString() ?? "";
        campoDescripcion = reg["descripcion"]?.ToString() ?? "";
        campoEstado = reg["estado"]?.ToString() ?? "activo";
        mostrarFormulario = true;
        mensaje = string.Empty;
    }

    private async Task GuardarRegistro()
    {
        if (string.IsNullOrEmpty(campoNombre))
        {
            mensaje = "El nombre es requerido";
            return;
        }
        var datos = new Dictionary<string, object?>
        {
            ["nombre"] = campoNombre,
            ["codigo"] = string.IsNullOrEmpty(campoCodigo) ? null : campoCodigo,
            ["descripcion"] = string.IsNullOrEmpty(campoDescripcion) ? null : campoDescripcion,
            ["estado"] = campoEstado
        };
        (bool exito, string mensaje) resultado;
        if (editando)
        {
            datos.Remove("codigo");
            resultado = await Api.ActualizarAsync("programa_ac", "nombre", campoNombre, datos);
        }
        else
        {
            resultado = await Api.CrearAsync("programa_ac", datos);
        }
        exito = resultado.exito;
        mensaje = resultado.mensaje;
        if (exito)
        {
            mostrarFormulario = false;
            await CargarRegistros();
        }
    }

    private async Task ConfirmarEliminar(Dictionary<string, object?> reg)
    {
        if (await JSRuntime.InvokeAsync<bool>("confirm", "¿Estás seguro que deseas eliminar este registro?"))
        {
            await EliminarRegistro(reg);
        }
=======
        registros = await Api.ListarAsync("programa_ac", limite);
        cargando = false;
    }

    private void NuevoRegistro() { campoPrograma = 0; campoAreaConocimiento = 0; mostrarFormulario = true; mensaje = string.Empty; }

    private async Task GuardarRegistro()
    {
        var datos = new Dictionary<string, object?> { ["programa"] = campoPrograma, ["area_conocimiento"] = campoAreaConocimiento };
        var resultado = await Api.CrearAsync("programa_ac", datos);
        exito = resultado.exito; mensaje = resultado.mensaje;
        if (exito) { mostrarFormulario = false; await CargarRegistros(); }
>>>>>>> SantiagoEcheverriDev
    }

    private async Task EliminarRegistro(Dictionary<string, object?> reg)
    {
<<<<<<< HEAD
        string nombre = reg["nombre"]?.ToString() ?? "";
        var resultado = await Api.EliminarAsync("programa_ac", "nombre", nombre);
        exito = resultado.exito;
        mensaje = resultado.mensaje;
        if (exito)
        {
            await CargarRegistros();
        }
    }

    private void Cancelar()
    {
        mostrarFormulario = false;
        mensaje = string.Empty;
    }

    [Inject] IJSRuntime JSRuntime { get; set; } = null!;
}
=======
        string programa = reg.GetValueOrDefault("programa")?.ToString() ?? "";
        var resultado = await Api.EliminarAsync("programa_ac", "programa", programa);
        exito = resultado.exito; mensaje = resultado.mensaje;
        if (exito) await CargarRegistros();
    }

    private void Cancelar() { mostrarFormulario = false; mensaje = string.Empty; }
}
>>>>>>> SantiagoEcheverriDev
